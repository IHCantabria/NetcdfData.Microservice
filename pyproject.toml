[project]
name = "NetcdfMicroservice"
requires-python = ">=3.7"
dynamic = ["version", "description"]

[project.optional-dependencies]
dev = ["black", "sphinx", "coverage[toml]", "myst-nb", "sphinx-autoapi", "sphinx_rtd_theme"]

[tool.black]
addopts = "-v"